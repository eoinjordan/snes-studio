# GitHub Pages

The web app can be deployed as a static GitHub Pages site.

It supports:

- sample project loading
- browser-only editing state
- helper patch proposals
- human review
- `.snesproj` download

It does not support:

- Python backend execution
- PVSnesLib builds
- ROM compilation
